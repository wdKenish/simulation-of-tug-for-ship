在一个二维平面中，有一个矩形（船体的一个简化模型）
受到风力的作用（阻力，方向固定切大小恒定）
输入几个坐标点，进行路线的拟合（样条曲线进行拟合）
小船初始状态知道其大小、质量、坐标、航向
风的作用力的计算公式为：
要求小船每个时刻都与样条曲线相切
根据样条曲线的末端的切线和处事的航向，计算改变的航向角，
通过给的靠泊时间计算出角速度
靠泊时间是怎么给定的呢？通过整体的靠泊速度来确定时间总量
这里的时间是到达转向点的时间，不包括船舶平行靠泊的时间
为了使船舶能够沿着路径（样条曲线）来进行运动，是每个时刻都需要提供动力的
计算每个时刻的动力的大小和方向，
最后显示为一个图（左边的y方向坐标轴是动力的方向180到0再到-180，
右边的y方向坐标轴是动力的大小的坐标轴，横向的坐标轴则表示为时刻即时间坐标轴）
其中可以简单使用牛顿运动定理来计算船舶的加速的，进而求解出船舶的速度，再进而求解出船舶的位移得到坐标位置
然后绘制每个时刻的船舶坐标位置的图，即船舶运动的轨迹图


更新版：
在一个二维平面中存在一个风速为10m/s，方向与x轴正方向为60度且指向原点方向的风阻力
有一个船舶在其中（简化为一个矩形）
船舶的长宽高：80、12、8
船舶的质量是5000t
初始位置（矩形中心的坐标）为：0，0
初始的速度（矩形中心的速度）：x方向0.2m/s，y方向0.1m/s
初始方向（矩形的长边与x轴的方向）：30度
航线的拟合点(0,0),(40,60),(120,200),(250,300),(400,400),(600,480)
根据这些点使用样条曲线来进行拟合航线
要求船舶在航行过程中都靠近拟合出来的航线运动，即航向尽量与航线相切
且求取出航线在到达最终拟合点处的切线斜率，计算在运动过程中船舶航向改变了多少
并计算出整体航线的长度
依据行进速度（最大合速度）不能超过5m/s，来计算总的时间
知道总的航向改变角和总时间计算出角速度的加速度（角速度的改变率）
风本身有一个方向，而船舶也有一个航向，所以需要计算船舶的风阻力时需要计算两个的遭遇角
例如在初始时刻时，风对船舶的前方有作用且两者的遭遇角是60度，对船的右侧方的遭遇角是30度
根据风的阻力计算公式：F=0.5*rho(the density of air)*s(the area of against to wind)*V(the speed of wind)**2*math.sin(theta(the angle of against of ship and wind))
在前方的面积是宽与高的乘积，侧方的面积是长与高的乘积。
然而每个时刻的船舶航向都有所改变，因此每个时刻的遭遇角不同，所以阻力大小不同
所以每个时刻的船舶运动所需要的动力的大小和方向也不同。
最终输出每个时刻的动力方向和大小的双坐标轴以时间为横轴的图以及每个时刻对应的船舶位置即轨迹图，还有每个时刻的航向图
请思考仔细使用python如何实现。并给出具体的实现代码


现在考虑当船舶到达最终点后速度为零
即当船舶快要到达目的点的时候计算船舶需要多大的动力来使得船舶减速
使得船舶到达目的点的时候船舶的速度恰好为零

第二次更新版：
要求在船舶到达最终拟合点的时候速度恰好为零
即在距离最终目标位置有一段距离时恰好能够提供相应的动力来使船舶减速，最终到达目标点是速度恰好为零













到达转向点后，在进行平行靠泊的计算